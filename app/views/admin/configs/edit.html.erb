<%= form_for @config,:url=>admin_config_path,:method=>:put,:html => {:class => "form-horizontal"} do |f| %>
  <fieldset>

    <div class="control-group">
      <%= f.label :name, "名称", :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :name%>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :cover_url, "key", :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :key,:readonly=>true%>
        <span class="help-inline"><%=@config.errors[:key].join(",")%></span>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :value, "value", :class => "control-label " %>
      <div class="controls">
        <%= f.text_field :value,:class=>"span6"%>
      </div>
    </div>

    <div class="control-group">
      <div class="controls">
        <%= f.submit '保存', :class => "btn btn-primary" %>&nbsp;&nbsp;&nbsp;&nbsp;
        <%= link_to '返回', admin_configs_path %>
      </div>
    </div>
  </fieldset>
<% end %>