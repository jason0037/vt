<h1>resources</h1>

<table>
  <tr>
    <th>resource</th>
    <th>description</th>
  </tr>

<% @resources.each do |resource| %>
  <tr>
    <td><%= resource.name %></td>
    <td><%= resource.description %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Resource', new_admin_resource_path %>
